from .. import loader, utils
import io
import os
from time import sleep

def register(cb):
	cb(Printer())
  
class Printer(loader.Module):
  strings = {'name': 'eCxb3'}
  
  async def prntt(self, message):
    """Используй .rnd <начало(сек)-конец(сек)>."""
    arg = utils.get_args_raw(event).split('-')
    await message.edit(arg)
